generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = "mongodb+srv://shafi:greenLizard03@practicecluster.1lndnyj.mongodb.net/car-mart-db?retryWrites=true&w=majority "
}

model Faq {
  id         String   @id @default(auto()) @map("_id") @db.ObjectId
  question   String
  answer     String
  createDate DateTime @default(now())
}

model Car {
  id         String @id @default(auto()) @map("_id") @db.ObjectId
  make       String
  model      String
  year       Int
  kilometers Int
  fuelType   String
  price      Int
  photoUrl   String
  details    String
}
